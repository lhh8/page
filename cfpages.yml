# Cloudflare Pages 配置文件
# 明确指定这是一个 Pages 项目

# 构建设置
build:
  command: "echo 'Static site - no build needed'"
  
# 部署设置
deployment:
  command: "npx wrangler pages deploy . --project-name xianyuziyuan --commit-dirty=true"